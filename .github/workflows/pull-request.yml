name: pull request

on: pull_request

jobs:

  build:

    runs-on: ubuntu-latest
    
    env:
      DOCKER_BUILDKIT: 1

    steps:
    - name: path
      run: pwd

    - name: view
      run: sudo ls

    - name: version
      run: cat ./appversion.json
